; Script generated by the HM NIS Edit Script Wizard.

; HM NIS Edit Wizard helper defines
!define PRODUCT_NAME "CamTracker"
!define PRODUCT_VERSION "2.0"
!define PRODUCT_PUBLISHER "Alex Hu"
!define PRODUCT_WEB_SITE "http://www.gzrobot.net"
!define PRODUCT_DIR_REGKEY "Software\Microsoft\Windows\CurrentVersion\App Paths\CamTracker.exe"
!define PRODUCT_UNINST_KEY "Software\Microsoft\Windows\CurrentVersion\Uninstall\${PRODUCT_NAME}"
!define PRODUCT_UNINST_ROOT_KEY "HKLM"

; MUI 1.67 compatible ------
!include "MUI.nsh"

; MUI Settings
!define MUI_ABORTWARNING
!define MUI_ICON "${NSISDIR}\Contrib\Graphics\Icons\modern-install.ico"
!define MUI_UNICON "${NSISDIR}\Contrib\Graphics\Icons\modern-uninstall.ico"

; Welcome page
!insertmacro MUI_PAGE_WELCOME
; License page
!insertmacro MUI_PAGE_LICENSE "..\OpenCV\CamTracker\bin\Release\Licence.txt"
; Directory page
!insertmacro MUI_PAGE_DIRECTORY
; Instfiles page
!insertmacro MUI_PAGE_INSTFILES
; Finish page
!define MUI_FINISHPAGE_RUN "$INSTDIR\CamTracker.exe"
!define MUI_FINISHPAGE_SHOWREADME "$INSTDIR\User's Manual.pdf"
!insertmacro MUI_PAGE_FINISH

; Uninstaller pages
!insertmacro MUI_UNPAGE_INSTFILES

; Language files
!insertmacro MUI_LANGUAGE "SimpChinese"

; MUI end ------

Name "${PRODUCT_NAME} ${PRODUCT_VERSION}"
OutFile "Setup.exe"
InstallDir "$PROGRAMFILES\CamTracker"
InstallDirRegKey HKLM "${PRODUCT_DIR_REGKEY}" ""
ShowInstDetails show
ShowUnInstDetails show

Section "MainSection" SEC01
  SetOutPath "$INSTDIR"
  SetOverwrite ifnewer
  File "..\OpenCV\CamTracker\bin\Release\CamTracker.exe"
  CreateDirectory "$SMPROGRAMS\CamTracker"
  CreateShortCut "$SMPROGRAMS\CamTracker\CamTracker.lnk" "$INSTDIR\CamTracker.exe"
  CreateShortCut "$DESKTOP\CamTracker.lnk" "$INSTDIR\CamTracker.exe"
  File "..\OpenCV\CamTracker\bin\Release\User's Manual.pdf"
  SetOutPath "$SYSDIR"
  File "D:\opencv\MinGW\install\x86\mingw\bin\libopencv_imgproc2413.dll"
  File "D:\opencv\MinGW\install\x86\mingw\bin\libopencv_highgui2413.dll"
  File "D:\opencv\MinGW\install\x86\mingw\bin\libopencv_core2413.dll"
  File "D:\opencv\MinGW\install\x86\mingw\bin\libopencv_video2413.dll"
  File "D:\opencv\MinGW\install\x86\mingw\bin\opencv_ffmpeg2413.dll"
SectionEnd

Section -AdditionalIcons
  SetOutPath $INSTDIR
  WriteIniStr "$INSTDIR\${PRODUCT_NAME}.url" "InternetShortcut" "URL" "${PRODUCT_WEB_SITE}"
  CreateShortCut "$SMPROGRAMS\CamTracker\Website.lnk" "$INSTDIR\${PRODUCT_NAME}.url"
  CreateShortCut "$SMPROGRAMS\CamTracker\Uninstall.lnk" "$INSTDIR\uninst.exe"
SectionEnd

Section -Post
  WriteUninstaller "$INSTDIR\uninst.exe"
  WriteRegStr HKLM "${PRODUCT_DIR_REGKEY}" "" "$INSTDIR\CamTracker.exe"
  WriteRegStr ${PRODUCT_UNINST_ROOT_KEY} "${PRODUCT_UNINST_KEY}" "DisplayName" "$(^Name)"
  WriteRegStr ${PRODUCT_UNINST_ROOT_KEY} "${PRODUCT_UNINST_KEY}" "UninstallString" "$INSTDIR\uninst.exe"
  WriteRegStr ${PRODUCT_UNINST_ROOT_KEY} "${PRODUCT_UNINST_KEY}" "DisplayIcon" "$INSTDIR\CamTracker.exe"
  WriteRegStr ${PRODUCT_UNINST_ROOT_KEY} "${PRODUCT_UNINST_KEY}" "DisplayVersion" "${PRODUCT_VERSION}"
  WriteRegStr ${PRODUCT_UNINST_ROOT_KEY} "${PRODUCT_UNINST_KEY}" "URLInfoAbout" "${PRODUCT_WEB_SITE}"
  WriteRegStr ${PRODUCT_UNINST_ROOT_KEY} "${PRODUCT_UNINST_KEY}" "Publisher" "${PRODUCT_PUBLISHER}"
SectionEnd


Function un.onUninstSuccess
  HideWindow
  MessageBox MB_ICONINFORMATION|MB_OK "$(^Name) 已成功地从你的计算机移除。"
FunctionEnd

Function un.onInit
  MessageBox MB_ICONQUESTION|MB_YESNO|MB_DEFBUTTON2 "你确实要完全移除 $(^Name) ，其及所有的组件？" IDYES +2
  Abort
FunctionEnd

Section Uninstall
  Delete "$INSTDIR\${PRODUCT_NAME}.url"
  Delete "$INSTDIR\uninst.exe"
  Delete "$SYSDIR\opencv_ffmpeg2413.dll"
  Delete "$SYSDIR\libopencv_video2413.dll"
  Delete "$SYSDIR\libopencv_core2413.dll"
  Delete "$SYSDIR\libopencv_highgui2413.dll"
  Delete "$SYSDIR\libopencv_imgproc2413.dll"
  Delete "$INSTDIR\User's Manual.pdf"
  Delete "$INSTDIR\CamTracker.exe"

  Delete "$SMPROGRAMS\CamTracker\Uninstall.lnk"
  Delete "$SMPROGRAMS\CamTracker\Website.lnk"
  Delete "$DESKTOP\CamTracker.lnk"
  Delete "$SMPROGRAMS\CamTracker\CamTracker.lnk"

  RMDir "$SMPROGRAMS\CamTracker"
  RMDir "$INSTDIR"

  DeleteRegKey ${PRODUCT_UNINST_ROOT_KEY} "${PRODUCT_UNINST_KEY}"
  DeleteRegKey HKLM "${PRODUCT_DIR_REGKEY}"
  SetAutoClose true
SectionEnd